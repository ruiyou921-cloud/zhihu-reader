# 贡献指南

感谢你改进知乎阅读。提交修改前，请确保改动保持“安静阅读、最少权限、只读交互”的产品边界。

## 本地开发

环境要求：Node.js 20、npm，以及 VS Code 1.85 或更高版本。

```bash
npm ci
npm run check
```

可在 VS Code 中按 `F5` 启动扩展宿主进行人工验证。

## 提交要求

- 一个提交聚焦一个明确问题，避免混入无关格式化或生成文件。
- 不提交 `node_modules`、`out`、`.vsix`、压缩包、日志或本地编辑器配置。
- 新功能和缺陷修复应增加相应测试。
- 代码、注释、界面文案和项目文档使用中文；协议字段及必要技术标识除外。
- 不得在代码、测试、Issue、提交信息或截图中包含真实 Cookie 和其他会话凭据。

## 安全不变量

涉及网络、身份认证和 Webview 的修改必须保持以下约束：

- 携带 Cookie 前校验 HTTPS、知乎主机和允许的 API 路径；
- 禁止把 Cookie 注入 Webview、日志或普通配置；
- 评论及内容读取保持 `GET` 请求，不增加写入知乎账号状态的操作；
- 远端分页地址在每次请求前重新校验；
- Webview 内容继续经过受控转换，不直接执行知乎返回的 HTML 或脚本。

## Pull Request

PR 描述请说明改动原因、用户影响、验证方法和可能风险。涉及界面调整时，建议附上不含个人信息的前后对比截图。

# 更新记录

## 0.8.1

- 补齐 GitHub 持续集成、安全报告、贡献指南和仓库元数据。
- 清理旧安装包与第三方 Cookie 工具推荐，强化公开发布时的凭据保护说明。

## 0.8.0

- 修复推荐接口地址被误当作网页原文地址，导致原文按钮持续提示“不安全”的问题。
- 回答、问题、文章和想法优先使用实体 ID 生成标准知乎网页地址；未知类型不再暴露 API 地址。
- 默认过滤推荐流中带有明确广告或推广结构标记的条目，并提供 `zhihuReader.filterAds` 开关。
- 广告识别不使用标题或正文关键词，避免误删讨论广告话题的正常内容。

## 0.7.1

- 修复扩展详情页 Logo 圆角外侧被保存为不透明浅灰色、形成白边的问题。
- 图标外侧改为真正透明的 Alpha 通道，保留深色圆角主体与柔和阴影。

## 0.7.0

- 删除回答正文上方和元数据中的重复“回答”标签。
- 将作者、时间、点赞压缩为单行轻量元数据，减少窄侧栏换行。
- 重绘评论气泡为标准 SVG 轮廓图标，并与元数据垂直居中。
- 更换活动栏开卷图标，并新增 256×256 扩展详情页 PNG 图标。
- 新增 1200×675 README 介绍图和扩展详情页品牌横幅配置。
- 更新扩展名称、简介与搜索关键词，使功能定位更直白。

## 0.6.1

- 将混在元信息标签中的评论入口改为独立的对话气泡图标按钮，并在图标旁保留评论数。
- 增加明确的悬停状态、提示文字和无障碍标签，让用户可以立即识别评论入口可点击。
- 移除评论读取成功后的接口版本字样；接口诊断只在失败时出现。

## 0.6.0

- 将回答元信息中的评论数量改为只读入口，点击后在独立抽屉中按需读取评论。
- 旧版回答评论接口返回 403 或 404 时，自动尝试新版 `comment_v5` 根评论接口。
- 评论请求固定使用 `GET`，并对回答 ID、评论端点及远端分页地址进行白名单校验。
- 评论正文不落盘、不预取，不提供发布、回复、点赞或删除操作。
- 评论接口失败时显示旧版与新版的 HTTP 诊断结果，便于真实账号环境验证。
- 将顶部阅读进度直条从醒目蓝色改为低对比度主题灰色。

## 0.5.1

- 将字号和行距控制板改为顶部栏下方的浮动面板，不再占用阅读区域高度。
- 增加面板关闭按钮，并支持点击空白处或按 `Esc` 收起。

## 0.5.0

- 在顶部栏边框操作区增加原文按钮，不占用正文空间，并校验知乎页面白名单。
- 增加 Cookie 状态指示和视图标题栏的清除登录入口。
- 增加 J/K、N/P、R、O、Esc 键盘操作。
- 增加两阶段滚动提示、翻页冷却和首尾内容提示，降低误触概率。
- 重构安全 Markdown 渲染，支持强调、行内代码、嵌套列表、引用、表格和代码复制。
- 增加阅读进度条，以及顶部栏字号和行距设置面板。
- 翻页失败时保留当前正文并显示临时通知。
- 增加 Webview 脚本语法、顶部按钮位置和 UI 功能静态测试。
- 按产品范围约束，不增加阅读历史。

## 0.4.0

- 校验知乎分页地址，只允许安全的 HTTPS API 地址携带 Cookie。
- 禁止自动跟随接口重定向，并为网络请求增加 15 秒超时。
- 增加“清除登录信息”命令，清除 Cookie 时同步重置阅读状态。
- 限制请求间隔和分页大小，兼容后续推荐页的 `paging.next`。
- 修复问题原文地址、非规则表格和侧边栏有序列表显示。
- 修复跨阅读模式预取任务可能互相覆盖的竞态。
- 增加网络安全、分页和 HTML 转换自动化测试。
- 使用 esbuild 生成单文件扩展入口，清理未使用的旧 Markdown 预览代码与冗余打包内容。

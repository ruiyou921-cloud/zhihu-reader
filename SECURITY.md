# 安全政策

## 支持范围

当前维护最新的次版本。安全修复会优先进入最新版本，不保证为旧版单独发布补丁。

## 报告安全问题

请使用 GitHub 仓库的私有安全报告功能提交漏洞，不要先创建公开 Issue。报告中建议包括：

- 受影响版本和运行环境；
- 可复现的最小步骤；
- 可能的影响和建议修复方向；
- 已移除登录信息的日志或截图。

请勿提交知乎 Cookie、会话令牌、密码、验证码、私钥或其他真实凭据。如果凭据已经泄露，应立即退出对应登录会话并在知乎侧撤销它。

## 安全边界

- Cookie 仅保存到 VS Code `SecretStorage`，不写入工作区和阅读历史。
- 携带 Cookie 的请求仅允许发往经过协议、主机和路径校验的知乎 API。
- 评论功能保持只读，只发送 `GET` 请求，不包含发布、回复、点赞或删除操作。
- 原文入口仅允许打开白名单内的知乎 HTTPS 网页。
- 本项目不提供绕过验证码、风控或访问控制的功能。

接口行为或知乎策略发生变化时，请先停止使用有疑问的功能，再提交不含凭据的报告。
